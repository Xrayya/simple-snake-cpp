# simple-snake-cpp
My attempt for my childhood simple snake game using C++, but this time with proper setup

## Building

I personally use separate `build/` directory to keep working directory clean:

```bash
mkdir build/
cd  build
cmake ..
cmake --build .

```
